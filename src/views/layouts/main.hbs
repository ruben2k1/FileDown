<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>FileDown - Inicio</title>
    <link rel="stylesheet" href="/public/css/index.css">
    <link rel="shortcut icon" href="/public/favicon.ico" type="image/x-icon">
    {{> bootstrap }}
</head>
<body>
    {{> nav }}
    
    {{> aside }}

    <h2>Archivos JPG</h2>

    <section>
        {{#each jpg}}
            <article>
                <ul>
                    <li><img src="{{this.RUTA_IMG}}" alt=""></li>
                    <li>{{this.DESCRIPCION}}</li>
                    <button><a href="{{this.RUTA_URL}}">DESCARGAR</a></button>
                </ul>
            </article>
        {{/each}}
    </section>

    <h2>Archivos RAR</h2>

    <section>
        {{#each rar}}
            <article>
                <ul>
                    <li><img src="{{this.RUTA_IMG}}" alt=""></li>
                    <li>{{this.DESCRIPCION}}</li>
                    <button><a href="{{this.RUTA_URL}}">DESCARGAR</a></button>
                </ul>
            </article>
        {{/each}}
    </section>

    <h2>Archivos PDF</h2>

    <section>
        {{#each pdf}}
            <article>
                <ul>
                    <li><img src="{{this.RUTA_IMG}}" alt=""></li>
                    <li>{{this.DESCRIPCION}}</li>
                    <button><a href="{{this.RUTA_URL}}">DESCARGAR</a></button>
                </ul>
            </article>
        {{/each}}
    </section>

    <h2>Archivos XLS</h2>

    <section>
        {{#each xls}}
            <article>
                <ul>
                    <li><img src="{{this.RUTA_IMG}}" alt=""></li>
                    <li>{{this.DESCRIPCION}}</li>
                    <button><a href="{{this.RUTA_URL}}">DESCARGAR</a></button>
                </ul>
            </article>
        {{/each}}
    </section>
</body>
</html>